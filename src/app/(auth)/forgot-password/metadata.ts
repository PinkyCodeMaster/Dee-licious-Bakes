import { BAKERY_META } from "@/lib/constants/brand";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: `Reset Password | ${BAKERY_META.title}`,
  description: `Reset your ${BAKERY_META.title} account password to regain access to your bakery account.`,
};